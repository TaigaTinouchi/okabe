# クライアント認証用の静的トークン。生成例: openssl rand -base64 32
AUTH_TOKEN=

# 待ち受けポート（省略時 8787）
PORT=8787

# SQLite ファイルのパス（省略時 data/okabe.db）
DB_PATH=data/okabe.db

# --- LLM (M2) ---
# 未設定の場合はエコー応答で起動する（M1 相当の動作確認用）
ANTHROPIC_API_KEY=

# 使用モデル（省略時 claude-opus-4-8）
# ANTHROPIC_MODEL=claude-opus-4-8
